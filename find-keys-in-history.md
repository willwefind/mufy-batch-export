# 找回「作者把卡设为私密」之后消失的角色 —— 零基础教程

> 作者把角色卡设为私密之后，那个角色会**从你的「聊过的角色」列表里整个消失**，
> mufy 网页上也打不开了。
>
> **但你和它的聊天记录还在服务器上**，只是没有入口了 —— 这份教程就是教你把入口找回来。

---

## 先看看你属于哪一种

| 你的情况 | 往哪走 |
|---|---|
| **我手上有那个角色的 roleId**（比如从别人发的清单里抄到的） | 走 **方法一**，五分钟搞定，**什么都不用装** |
| **我不知道是哪些角色没了，想一次全捞出来** | 走 **方法二**，要装一个 Python（十分钟） |
| **我只有手机** | 看最后一节 **「手机怎么办」** |

> 💡 **大多数人走方法一就够了。** 方法二是给「想一次挖干净」的人准备的。

---

# 方法一：只用浏览器（不装任何东西）

## 第 1 步：打开历史记录

- **电脑 Chrome / Edge**：按 **`Ctrl` + `H`**
- **Mac**：按 **`⌘` + `Y`**

## 第 2 步：搜那一整串 roleId

把 roleId **整串**粘进历史记录的搜索框，比如：

```
xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
```

（就是清单里给你的那一整串，32 个字母数字 + 4 个横杠。）

> 🔴 **一定要搜这一整串（或者至少前 8 位），不要只搜 `roleId` 这个词。**
>
> 搜 `roleId` 你只会看到七八条，会以为"我历史里就这么点"——**那是假象**。
> mufy 每个页面的标题都一样（都叫 `Mufy-mumu`），
> Chrome 会把同一天、同一个标题的记录**折叠成一行**，所以几百条被折成了"每天一条"。
> 实测过：界面上显示 8 条，实际库里有 **791 条**。
>
> 搜完整的 ID 就能精确命中，不受折叠影响。

## 第 3 步：把地址复制下来

搜出来的每一条，都是你和这个角色的一段聊天页。

- **电脑**：右键那一条 →「**复制链接地址**」
- 复制到的东西长这样（两串 ID 都在里面）：

```
https://……/chat?roleId=xxxxxxxx-…&sessionId=yyyyyyyy-…
```

> ⚠️ **一个角色可能有好几段对话**，就是好几条不同的记录。
> **每一条都要复制**，少复制一条就少救回一段。

## 第 4 步：粘进导出脚本

1. 装好 **mufy 批量导出脚本**（**v1.43 或更新**）：
   <https://github.com/willwefind/mufy-batch-export>
2. 回 mufy 页面，点左下角「⬇ 批量导出」；
3. **范围**选「**救回卡已消失的角色（粘聊天页地址）**」；
4. 把刚才复制的地址粘进那个大框里，**一行一条**；
5. 点「**开始导出**」。

脚本会自己去抓那几段的正文，打包下载给你。

> 💡 **不确定哪个角色消失了？没关系，多粘几条不吃亏。**
> 脚本会先拉一遍你的「聊过的角色」列表，**还在列表里的会自动挑掉并告诉你名字**，
> 只救真正消失的那些。

---

# 方法二：用小工具一次挖干净

**什么时候用它**：你不知道哪些角色没了，或者不想一条条右键复制。

它会直接读你电脑上的浏览器历史文件，把**所有** mufy 聊天页的钥匙一次性挖出来，
存成一个文本文件，你全选复制粘进面板就行。

> 🔒 **它只读你自己电脑上的文件，不联网、不上传、不改动任何东西。**
> 浏览器开着的时候历史文件是锁住的，所以它会先复制一份出来读，用完立刻删掉。

## 第 1 步：装 Python（只要装一次）

1. 打开 <https://www.python.org/downloads/>，点那个大大的黄色按钮下载；
2. 双击安装，**第一个页面的最下面有一行 `Add Python to PATH`（把 Python 添加到环境变量），
   一定要打勾** ——
   🔴 **这一步没勾，后面一定会报「不是内部或外部命令」**；
3. 然后一路「下一步 / Install Now」到底。

## 第 2 步：把小工具放好

把 `find-keys-in-history.py` 这个文件放到一个**你找得到的文件夹**里，
比如在桌面上新建一个文件夹叫 `mufy`，放进去。

## 第 3 步：在那个文件夹里打开命令行

**最省事的办法（Windows）**：

1. 打开那个文件夹；
2. 在**地址栏**（窗口上方显示当前文件夹路径的那一条）里，把路径**全删掉**，输入 `cmd`，回车；
3. 会跳出一个黑色窗口，开头就是你那个文件夹的路径 —— 对了。

**Mac**：打开「终端」，输入 `cd ` （**cd 后面有个空格**），
然后把那个文件夹**拖进终端窗口**，回车。

## 第 4 步：运行

在黑窗口里输入这一行，回车：

```
python find-keys-in-history.py
```

> 💡 **不想打中文文件名？** 输入 `python ` （后面留个空格），
> 然后**把那个 .py 文件直接拖进黑窗口**，路径会自动填进去，再回车。
> 这一招对付中文文件名最好使。

跑完你会看到类似这样：

```
扫过：Chrome/Default(1128)、Edge/Default(1026)
mufy 相关网址 2154 条 → 带 roleId 的去重后 742 组
涉及 215 个角色（其中 72 个有不止一段对话），共 570 段。

✅ 写好了：……\_历史里的钥匙.txt（629 行）
```

## 第 5 步：粘进面板

用**记事本**打开刚生成的 `_历史里的钥匙.txt` →
**全选（`Ctrl`+`A`）→ 复制（`Ctrl`+`C`）** →
到 mufy 的导出面板，范围选「**救回卡已消失的角色**」→ 粘进去 → 「开始导出」。

还在你列表里的角色，脚本会自动挑掉，所以**整份粘进去就行，不用挑**。

## 只想找某一个角色

在命令后面加上那个 roleId（前 8 位就够）：

```
python find-keys-in-history.py xxxxxxxx
```

---

## 出错了怎么办

| 你看到的 | 怎么回事 |
|---|---|
| `'python' 不是内部或外部命令` | 装 Python 时**没勾 `Add Python to PATH`**。重装一次，记得勾上。 |
| `找不到文件` / `No such file` | 黑窗口现在不在那个文件夹里。回到第 3 步；或者用「把文件拖进窗口」那一招。 |
| 窗口一闪就没了 | 你是**双击**运行的。按第 3、4 步从命令行跑，就能看到结果了。 |
| `❌ 没找到任何浏览器的历史文件` | 你用的浏览器不在支持名单里（支持 Chrome / Edge / Brave / 360极速 / QQ浏览器）。 |
| `❌ 历史里没有以 xxx 开头的 roleId` | 这个浏览器的历史里没有和它的聊天记录。**换个浏览器再跑一次**——很多人在 Chrome 和 Edge 上都聊过。 |
| 挖出来的比想象中少 | 浏览器历史一般**只留 90 天**；清过历史、换过电脑就没有了。 |

---

## 手机怎么办

**手机上读不了历史数据库**（系统不让），所以方法二用不了。但方法一照样能用：

| 手机 | 打开历史记录 |
|---|---|
| **安卓** Chrome / Edge | 右上角 **⋮** →「历史记录」 |
| **安卓** Via / X浏览器 / 夸克等 | 菜单里找「历史」 |
| **iPhone** Safari | 底部 **📖** 书本图标 → 上方 **🕐** 时钟页签 |
| **iPhone** Chrome | 右下角 **⋯** →「历史记录」 |

进去之后**搜那一整串 roleId**，找到那几条：

- 长按 → 菜单里一般有「复制链接」；
- 复制不了就**点开它** —— 对话虽然打不开，但**地址栏里那串就是你要的**，从地址栏复制一样管用。

> 💡 **电脑上聊过的话，去电脑上找更省事。** 历史是各设备各存各的。

---

## 🔴 最后一件要紧事

复制到的地址里有两串 ID，**性质完全不同**：

| | 是什么 | 能不能公开 |
|---|---|---|
| `roleId=` | **角色卡的编号**，全站共用，谁看到都一样 | ✅ 可以，别人靠它才能搜自己的记录 |
| `sessionId=` | **你和这个角色那一段对话的编号**，是你自己的东西 | 🔴 **不要公开** |

想帮别人的话，**只发 roleId 就够了** —— 每个人拿它去搜自己的历史，
搜出来的是**他们自己的** sessionId。
